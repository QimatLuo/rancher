apiVersion: apps/v1
kind: Deployment
metadata:
  name: android-tv
spec:
  selector:
    matchLabels:
      app: android-tv
  template:
    metadata:
      labels:
        app: android-tv
    spec:
      hostNetwork: false
      containers:
        - name: adb
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - sh
            - -c
            - bash connect.sh && exec python3 -u match_click.py
          env:
            - name: ADB_SERVER_IP
              value: "192.168.1.163"
            - name: ADB_SERVER_PORT
              value: "5555"
            - name: MATCH_ACTION
              value: dpad_center
            - name: MATCH_AFTER_TAP_SLEEP
              value: "5"
            - name: MATCH_THRESHOLD
              value: "12"
            - name: MATCH_TEMPLATE_DIR
              value: /sample_click/tv
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          securityContext:
            privileged: true
          volumeMounts:
            - name: android
              mountPath: /root/.android
            - name: src
              mountPath: /src
            - name: sample-click
              mountPath: {{ .Values.persistence.mountPath }}
          workingDir: /src
      volumes:
        - name: android
          configMap:
            name: android
        - name: src
          configMap:
            name: src
        - name: sample-click
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.claimName }}
