apiVersion: v1
kind: ConfigMap
metadata:
  name: local-llm-proxy-config
data:
  config.yaml: |
    model_list:
      - model_name: "tinyllama"
        litellm_params:
          model: "ollama/tinyllama"
          api_base: "http://local-llm.local-llm.svc.cluster.local:11434"
