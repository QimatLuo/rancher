apiVersion: v1
kind: Service
metadata:
  name: internal-registry
  labels:
    app.kubernetes.io/name: internal-registry
    app.kubernetes.io/component: registry
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: internal-registry
    app.kubernetes.io/component: registry
  ports:
    - name: registry
      port: 5000
      targetPort: 5000
      nodePort: 30500
